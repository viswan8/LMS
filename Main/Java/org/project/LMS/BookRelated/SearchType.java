package Main.Java.org.project.LMS.BookRelated;

/**
 * Defines the criteria by which books can be searched in the library system.
 * This enum provides a clear, type-safe way to specify search parameters.
 */
public enum SearchType {
    BY_TITLE,
    BY_AUTHOR,
    BY_ISBN
}
